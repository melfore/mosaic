import { DialogTitle as MUIDialogTitle, styled } from "@material-ui/core";

export const StyledMUIDialogTitle = styled(MUIDialogTitle)({
  alignItems: "center",
  display: "flex",
  justifyContent: "space-between",
});
